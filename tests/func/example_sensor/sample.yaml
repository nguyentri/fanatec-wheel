# Twister entry for the example sensor functional test (template).
sample:
  description: Example application (custom driver class template)
  name: example-sensor
common:
  build_only: true
  integration_platforms:
    - fk723m1_zgt6
tests:
  app.default: {}
  app.debug:
    extra_overlay_confs:
      - debug.conf
