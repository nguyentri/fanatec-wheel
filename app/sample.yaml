# Twister entry for the rim firmware.
sample:
  description: Rim-side firmware (legacy 33-byte SPI link)
  name: rim-app
common:
  build_only: true
  integration_platforms:
    - fk723m1_zgt6
tests:
  app.rim.default: {}
