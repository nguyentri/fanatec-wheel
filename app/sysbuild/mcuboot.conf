# MCUboot tuning for the rim (spec 5-S1/5-S2).
# Swap-using-move: dual-slot update with automatic revert on a failed
# self-test (docs/update-recovery.md); no scratch partition needed.
CONFIG_BOOT_SWAP_USING_MOVE=y
